# Minimal SearXNG config for the Engram harness research tools.
use_default_settings: true
server:
  secret_key: "engram-local-only-not-secret"
  limiter: false
search:
  formats:
    - html
    - json
