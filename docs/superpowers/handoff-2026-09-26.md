# Handoff: 2026-09-26 session

Continues `handoff-2026-09-23.md`. PR #53 merged, and PRs #54–57 (a frontend audit, a collapsible
side panel collapsed by default, a light/dark toggle, graph label packing) landed after it. This
session restarted `claude/gpt5-6-sol-ui-grade-lw6n1p` from that `master`, audited every screen in
both schemes and at 390px on a demo vault, and fixed what the audit found. No model keys again, and
egress still blocks openrouter.ai, so the live check with a real model is still undone.

## State

- `tsc --noEmit` clean.
- `vitest run` with `PLAYWRIGHT_CHROMIUM_PATH=/opt/pw-browsers/chromium`: 2747 passed, 3 failed,
  5 skipped. The same three container failures as 09-23: `atomicWrite` running as root, and two
  `liveModelSettings` tests that need openrouter.ai.
- `npx playwright test --project=e2e`: 21 passed.
- `graph-perf.e2e.ts` fails here on its run cap (the 5,000-node layout lands about 20 worker
  iterations in 12 s under software WebGL). It fails the same way with the graph code from before
  `ea98717`, which passed once earlier the same day, so it is this container's speed, not a
  regression. It still needs the real-GPU run 09-23 asked for.

## What changed

- **Phone: one view at a time.** Under 640px an open side panel used to stack under the chat and
  leave both as strips. Now the panel fills the canvas and its collapse button reads "Chat", with a
  dot and a changed accessible name when the tutor answered while the panel was open. The collapsed
  rail is a labelled bottom bar. Focus mode keeps the old stack. `workspace-polish` and `stage-math`
  e2e specs follow the new layout.
- **Notebooks.** The heading no longer draws a focus ring after the route change moves focus to
  it. Filing and delete controls on a row come up on hover or focus (always visible on touch).
  Each conversation in a notebook lists up to four pages it reached (`pages` on the detail route's
  threads), which tells apart the identical titles the Studio buttons produce.
- **Conversation title** above the transcript, from `src/shared/threadTitle.ts`, which is
  `titleFor` moved out of `sessionStore.ts` so the history list and the header agree. Hidden on a
  phone.
- **Models dialog** opens on the tutor's provider and model. The other roles, every context
  window and their notes are under "other roles and context windows"; save is pinned to the foot.
- **Loading.tsx** is the one loading line across the Page tab, graph, notebooks and palette
  (backlog craft debt 2, closed).
- **Desktop app**: `build/icon.svg` and `build/icon.png` (backlog setup item 1, closed); the favicon
  uses the same colours; the windows are titled Myelin, not Engram.
- **One KaTeX.** An npm override points rehype-katex, remark-math and mermaid at the app's katex
  0.17. The entry bundle drops from 1,998 kB to 1,737 kB and a 258 kB duplicate chunk goes. Markdown
  maths was rendered by 0.16 under 0.17's stylesheet before. Mermaid's maths labels were not
  checked by eye.
- **Phone topbar**: the student and model badges show icons only under 640px; in a filed
  conversation the model badge had slid under Add material.

## Next, in order

1. The live check from 09-23, unchanged: a real model, a greeting, a research turn, `/study`, an
   aside, the Studio prompts read against real output, and the graph on a real vault.
2. `graph-perf.e2e.ts` on real GPU hardware.
3. A look at a mermaid diagram with maths in it, now on katex 0.17.

## Open decisions for the user

- Carried from 09-23: graph labels in 13–40-page views, whether a chat turn's grading continuation
  compiles into lesson notes, the shadowed `OPENAI_API_KEY`, and a citation-aware index for
  canonical sources.
- Which of the 20 chat UIs shown on 09-23 to follow, if any. This session kept the existing design
  language and did not pick one.
